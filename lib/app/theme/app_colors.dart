import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary - 따뜻한 브라운 계열 (책/나무 느낌)
  static const primary = Color(0xFF8B6914);
  static const primaryLight = Color(0xFFD4A843);
  static const primaryDark = Color(0xFF5C4510);

  // Secondary - 그린 계열 (환경/성장)
  static const secondary = Color(0xFF4A7C59);
  static const secondaryLight = Color(0xFF7DB88E);

  // Accent
  static const accent = Color(0xFFE8734A);

  // Neutral
  static const background = Color(0xFFF5F0E8);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF2D2416);
  static const textSecondary = Color(0xFF7A6B5A);
  static const divider = Color(0xFFE0D5C5);

  // Status
  static const success = Color(0xFF4A7C59);
  static const warning = Color(0xFFE8A834);
  static const error = Color(0xFFD64045);
  static const info = Color(0xFF4A90A4);

  // Temperature
  static const tempCold = Color(0xFF4A90A4);
  static const tempWarm = Color(0xFFE8A834);
  static const tempHot = Color(0xFFD64045);
}
